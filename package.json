{
  "name": "noelite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Elite's structure, Lander's face. A seeded universe you fly, land on, dig up and sell across.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "verify": "node tools/verify-terrain.mjs && node tools/verify-chunk.mjs && node tools/verify-flight.mjs && node tools/verify-system.mjs && node tools/verify-economy.mjs",
    "verify:lod": "node tools/verify-lod.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "three": "^0.185.1",
    "@types/three": "^0.185.4"
  }
}
